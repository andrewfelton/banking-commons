"""Shared utilities for the banking-policy projects.

Currently ships the `email` subpackage (ACS digest delivery). LLM and storage
helpers are planned — see docs/architecture.md.
"""

__version__ = "0.1.0"
